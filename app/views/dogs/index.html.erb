<div>
  <ul>
      <% @dogs.each do |dog| %>
        <img src="<%= dog.picture %>" alt="">
        <li>Name: <%= link_to dog.name, dog_path(dog.id) %> | Age: <%= dog.age %> | Location: <%= dog.location %> | Price: <%= dog.price %>€/h </li>
      <% end %>
  </ul>

<div class="btn btn-primary">
  <p>Create dog</p>
  <%# <%= link_to 'Edit', edit_product_path(@product), :class => "btn" %> |
  <%# <%= link_to 'Back', products_path, :class => "btn"%>
</div>
